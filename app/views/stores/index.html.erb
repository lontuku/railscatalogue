<p id="notice"><%= notice %></p>
<h1>Categorías</h1>
<% @categories.each do |c| %>
  <%= link_to c.name, stores_path(:categ => c.id) %> 
<% end %>
<h1>Stores</h1>

<table class="table table-striped table-hover">
  <thead>
    <tr>
      <th scope="col">#</th>
      <th scope="col">Nombre</th>
      <th scope="col" >IG Link</th>
      <th scope="col" class="text-center">Usuario</th>
      <th scope="col" class="text-center">Cerrar</th>
      <th scope="col"></th>
      <th scope="col"></th>
      <th scope="col"></th>
    </tr>
  </thead>

  <tbody id="listatiendas">
   <%= render 'list', stores: @stores %>
  </tbody>
</table>

<br>

<%= link_to 'New Store', new_store_path %>
